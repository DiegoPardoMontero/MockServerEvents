{
  "name": "mock-events-api",
  "version": "1.0.0",
  "description": "Mock API para eventos usando json-server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mock",
    "api",
    "json-server",
    "events"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4",
    "lowdb": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}